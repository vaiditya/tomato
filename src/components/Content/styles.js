import styled from "styled-components";

const ContentStyle = styled.div`
  .main-content {
    padding-top: 60px;

    .content {
      padding: 0px 40px;
      background: #f7f7f0;
    }
  }
`;

export default ContentStyle;
